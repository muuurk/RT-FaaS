version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  pidserver:
    lang: python3-edf
    handler: ./pidserver
    image: szefoka/pidserver:latest
    EDF:
      runtime: "50"
      deadline: "1000"
      period: 1000

